-- Drizzle Kit 0.31+ snapshot alignment: metadata for `profiles_display_name_unique` changed
-- (index column shape). The unique index already exists from `0002_workout_domain.sql`; no DDL.
SELECT 1;
