# Optional: absolute API origin when the SPA is hosted separately from the API
# (e.g. Vercel frontend + Render API). Omit for same-origin / local dev with Vite proxy.
# VITE_API_BASE_URL=https://your-api.onrender.com
