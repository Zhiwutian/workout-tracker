/** localStorage keys for Phase 1 display preferences (`wt-` prefix avoids clashes on shared origins). */
export const DISPLAY_STORAGE_KEYS = {
  textScale: 'wt-text-scale',
  highContrast: 'wt-high-contrast',
  darkMode: 'wt-dark-mode',
} as const;
